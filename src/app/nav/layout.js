
export const metadata = {
  title: "Nav Bar",
  description: "Happy booking",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body >
        {children}
      </body>
    </html>
  );
}
